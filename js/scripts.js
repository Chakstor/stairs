window.onload = () => {
    let button = document.getElementById('start');

    const game = new Game();
    game.init();

    // button.addEventListener('click', (e) => {
    //     const game = new Game();
    //           game.init();

    //     button.style.display = "none";
    // });
}

